<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GyroMouse.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GyroMouse"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GyroMouse">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="GyroMouse — Windows Receiver" 
               FontSize="20" Margin="0,0,0,12"/>
        <Button x:Name="TestMouseBtn" Content="Test Mouse (+50,+50)" 
        Click="TestMouseBtn_Click" Margin="0,0,0,8"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Sensitivity:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Slider x:Name="SensSlider" Minimum="50" Maximum="2000" Value="500" 
            Width="200" ValueChanged="SensSlider_ValueChanged"/>
            <TextBlock x:Name="SensLabel" Text="500" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="LogBox" TextWrapping="Wrap" FontFamily="Consolas"/>
        </ScrollViewer>
    </Grid>
</Window>
